```bash
# Make Sure HUB is installed — if you're using it.
brew install hub

# Make a project directory
mkdir <project name>

# Change Directory into project directory
cd <project name>

# Initialize git repo
git init

# Make remote repo on github (follow instructions)
hub create -d <description>


```



